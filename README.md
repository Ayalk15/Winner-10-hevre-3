# Winner 10 Hevre — Premium v5 Clean

גרסה נקייה משמות אישיים.

חשוב: אין Firebase / Database / LocalStorage. הנתונים נשמרים רק בזיכרון הדפדפן ולכן אחרי רענון הכול מתאפס.

## הרצה מקומית

```bash
npm install
npm run dev
```

## בנייה לפרסום

```bash
npm run build
```
